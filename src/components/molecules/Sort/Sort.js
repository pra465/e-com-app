import React from 'react';

const Sort = () => {
  return (
    <div>
      <select>
        <option>Default</option>
        <option>High to Low</option>
        <option>Low to high</option>
      </select>
    </div>
  );
};

export default Sort;
